name: Music Vis MQP Frontend
run-name: ${{ github.actor }} is hosting frontend
on: [push]
jobs:
  Add-New-Frontend-Feature:
    runs-on: ubuntu-latest
    steps:
      - run: npm install -g @angular/cli
      - run: ls -a
      - run: cd /home/runner/work/supabase-frontend/ && ng build --prod
      - run: ls -a
      

